{
  "name": "@keystone/pi",
  "version": "0.1.0",
  "type": "module",
  "description": "Pi extension for Keystone goal-driven development workflow",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:integration": "vitest run test/integration"
  },
  "piConfig": {
    "extensions": ["./src/index.ts"]
  },
  "peerDependencies": {
    "pi": ">=0.84.1"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.2.1"
  }
}
